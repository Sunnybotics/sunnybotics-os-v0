<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>sunnybotics_cmi</name>
  <version>0.1.0</version>
  <description>
    SunnyboticsOS Common Machine Interface (CMI). The hardware-agnostic contract
    every machine speaks: MachineState for what a machine is and how it is
    doing, and the Mission action for giving it work. Contains interface
    definitions only, deliberately, so that machine drivers and the OS adapter
    share a schema without sharing a dependency on each other.
  </description>
  <maintainer email="maintainers@sunnybotics.invalid">SunnyboticsOS Machine Layer</maintainer>
  <license>Apache-2.0</license>

  <buildtool_depend>ament_cmake</buildtool_depend>
  <buildtool_depend>rosidl_default_generators</buildtool_depend>

  <depend>builtin_interfaces</depend>
  <depend>action_msgs</depend>

  <exec_depend>rosidl_default_runtime</exec_depend>
  <member_of_group>rosidl_interface_packages</member_of_group>

  <test_depend>ament_lint_auto</test_depend>
  <test_depend>ament_lint_common</test_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
